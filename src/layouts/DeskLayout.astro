---
import PageLayout from './PageLayout.astro';
import { Picture } from 'astro:assets';

const { frontmatter } = Astro.props;
const formattedDate = frontmatter.date 
  ? new Date(frontmatter.date).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })
  : 'Unknown Date';
const isoDate = frontmatter.date
  ? new Date(frontmatter.date).toISOString() 
  : '';
---
<PageLayout 
  title={`Desk - ${formattedDate} - Thamara Kandabada`} 
  description={frontmatter.description} 
  image={frontmatter.imageUrl}
  feeds={[{ title: "Desk - Thamara Kandabada", href: "/desk/rss.xml" }]}
>

<article class="h-entry">
<a class="p-author h-card" href="https://thamara.co.uk/" hidden>Thamara Kandabada</a>
<a class="u-url" href={Astro.url.href} hidden>Permalink</a>
<h1 class="p-name" hidden>Desk Setup - {formattedDate}</h1>

    <div class="blog-header">
	  <p><span class="blog-date"><time class="dt-published" datetime={isoDate}>{formattedDate}</time></span>//<span class="blog-category p-category">{frontmatter.location}</span></p>
    </div>

	  <div class="blog-image">
	  <figure class="blog-hero-image">
      <Picture
        src={frontmatter.imageUrl}
		formats={['avif', 'webp']}
		alt={frontmatter.imageAlt}
      />
    </figure>
	  </div>
	  	  <p class="p-summary">{frontmatter.description}</p>
        <div class="e-content">
          <slot />
        </div>
          <div class="permalink">
  <p><strong>Permalink:</strong> <textarea>{Astro.url.href}</textarea></p>
  </div>
        </article>
</PageLayout>